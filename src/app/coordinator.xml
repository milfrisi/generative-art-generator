<coordinator-app
    start="${coordinatorStartDate}"
    end="${coordinatorEndDate}"
    frequency="${coord:days(1)}"
    name="${user}-${project}"
    timezone="UTC"
    xmlns="uri:oozie:coordinator:0.2">

    <action>
        <workflow>
            <app-path>/user/${user}/${project}/src/app/example_spark_wf/</app-path>
            <configuration>
                <property>
                    <name>oozie.use.system.libpath</name>
                    <value>True</value>
                </property>
                <property>
                    <name>crunchDate</name>
                    <value>${coord:formatTime(coord:dateOffset(coord:nominalTime(),-12,'HOUR'),'yyyyMMdd')}
                    </value>
                </property>
            </configuration>
        </workflow>
    </action>

</coordinator-app>
